diff --git a/packages/playwright-ct-svelte/registerSource.mjs b/packages/playwright-ct-svelte/registerSource.mjs
index a1b2c3d..e4f5g6h 100644
--- a/packages/playwright-ct-svelte/registerSource.mjs
+++ b/packages/playwright-ct-svelte/registerSource.mjs
@@ -30,7 +30,7 @@ function isObjectComponent(component) {
 }
 
 function isJSX(component) {
-  return typeof component === 'object' && component && component.__pw_type === 'object-component';
+  return typeof component === 'object' && component && component.__nx_type === 'object-component';
 }
 
 /**
@@ -52,7 +52,7 @@ export function importKey(key) {
   return key + '/__pw_component';
 }
 
-const __pwSvelteComponentKey = Symbol('svelteComponent');
+const __nxSvelteComponentKey = Symbol('svelteComponent');
 
 window.playwrightMount = async (component, rootElement, hooksConfig) => {
   if (!component.$$prop_def && !isObjectComponent(component))
@@ -73,13 +73,13 @@ window.playwrightMount = async (component, rootElement, hooksConfig) => {
     });
   }
 
-  for (const hook of window.__pw_hooks_before_mount || [])
+  for (const hook of window.__nx_hooks_before_mount || [])
     await hook({ hooksConfig });
 
   const svelteComponent = new component.value({ target: rootElement, props: params });
-  rootElement[__pwSvelteComponentKey] = svelteComponent;
+  rootElement[__nxSvelteComponentKey] = svelteComponent;
 
-  for (const hook of window.__pw_hooks_after_mount || [])
+  for (const hook of window.__nx_hooks_after_mount || [])
     await hook({ hooksConfig, svelteComponent });
 };
 
@@ -86,14 +86,14 @@ window.playwrightMount = async (component, rootElement, hooksConfig) => {
 };
 
 window.playwrightUnmount = async rootElement => {
-  const svelteComponent = /** @type {SvelteComponent} */ (rootElement[__pwSvelteComponentKey]);
+  const svelteComponent = /** @type {SvelteComponent} */ (rootElement[__nxSvelteComponentKey]);
   if (!svelteComponent)
     throw new Error('Component was not mounted');
-  delete rootElement[__pwSvelteComponentKey];
+  delete rootElement[__nxSvelteComponentKey];
   svelteComponent.$destroy();
 };
 
 window.playwrightUpdate = async (rootElement, component) => {
-  const svelteComponent = /** @type {SvelteComponent} */ (rootElement[__pwSvelteComponentKey]);
+  const svelteComponent = /** @type {SvelteComponent} */ (rootElement[__nxSvelteComponentKey]);
   if (!svelteComponent)
     throw new Error('Component was not mounted');